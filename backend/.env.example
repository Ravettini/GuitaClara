# Puerto del servidor (Railway lo configura automÃ¡ticamente)
PORT=3001

# URL del frontend en producciÃ³n
# Ejemplo: https://tu-app.vercel.app
FRONTEND_URL=https://tu-frontend.vercel.app

# Base de datos (Supabase PostgreSQL)
# Formato: postgresql://user:password@host:port/database?sslmode=require
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# JWT Secrets (generar valores seguros aleatorios)
JWT_SECRET=tu-jwt-secret-super-seguro-aqui
JWT_REFRESH_SECRET=tu-refresh-secret-super-seguro-aqui

# Tiempo de expiraciÃ³n de tokens
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Entorno
NODE_ENV=production

# API Keys opcionales (para servicios externos)
# MARKET_API_KEY=tu-api-key-si-la-necesitas
